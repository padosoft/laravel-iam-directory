node_modules/
_site/
*.log
.DS_Store

# docmd-search: keep the pinned model config, ignore build cache/index artifacts
.docmd-search/*
!.docmd-search/config.json
# the per-docs semantic index is a regenerable build artifact (rebuilt by `npm run build`)
docs/.docmd-search/
